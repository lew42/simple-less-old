import View, { el, div } from "/simple/View/View.js";

export default class PageView extends View {
	// content(){
	// 	return this.page.title + " (" + this.page.slug + ")";
	// }
}

PageView.prototype.classes = "page";